spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

rabbitmq:
  queue:
    name: "message_queue"
  exchange:
    name: "message_exchange"
  routing:
    key: "message_routing_key"
